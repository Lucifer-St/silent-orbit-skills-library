# Silent Orbit 旧版 v1 RC 单文件交接说明

这是兼容旧 beta 流程的历史入口。当前候选为 `v0.13.0-beta.1`，请从唯一 Release 页面 https://github.com/Lucifer-St/silent-orbit-skills-library/releases/tag/v0.13.0-beta.1 下载新的 `SILENT_ORBIT_NOVICE_HUMAN_TEST_PACK.zh-CN.md`。

兼容构建标识：`V1_RC_ONE_FILE_HANDOFF.zh-CN.md`；当前安装资产：`silent-orbit-skills-library-0.13.0-beta.1.tgz`。这两个名称仅用于旧自动化的供应链校验，不表示旧交接协议仍是本版测试入口。

旧回执标识 `SILENT_ORBIT_RETURN_REPORT_V1` 仅为已安装包 smoke 的向后兼容探针；本版真人回传必须使用新包中的 `SILENT_ORBIT_NOVICE_REPORT_JSON`。

新包由 Agent 主持只读 preflight、项目级同意门、逐题生活化访谈、两方向比较、默认视图、keep/refresh 和真实地图结构重做，并生成 `SILENT_ORBIT_NOVICE_REPORT_JSON` 单文件报告。不要用本说明冒充新的独立新手验收包。
