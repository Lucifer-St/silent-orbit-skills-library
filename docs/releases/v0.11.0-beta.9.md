# Silent Orbit v0.11.0-beta.9 — Windows generate access repair

`v0.11.0-beta.9` is a GitHub Pre-release candidate generated from the Private
source of truth through the deterministic Public Export.

## Why this candidate exists

An external `v0.11.0-beta.8` run passed the Release checksum and handoff
contract, then stopped at `generate` with a Windows `EIO: Access denied`
failure. The failed run left `dist` absent and the expected first diff at one
added Skill. This is a P1 core-flow defect, not an accepted v1 result.

The failure signature was reproduced against the immutable beta.8 tarball by
injecting the same Windows-style denial at the staged-directory publication
boundary.

A final pre-publication audit also found two release blockers outside the
generator implementation:

- checksum instructions could select beta.8 or the first unrelated row in
  `SHA256SUMS.txt`, and the POSIX command tried to verify optional assets that
  the reader had not been told to download;
- the bundled `build-skill-cosmos` CLI contract still required the historical
  `0.1.x` interface even though this package contains CLI `0.4.0`.

## Windows generation fix

- Retry transient `EACCES`, `EBUSY`, `EIO`, `ENOTEMPTY`, and `EPERM` failures
  while staging and publishing generated files.
- Fall back to a validated copy when Windows repeatedly denies the final
  directory rename and no previous `dist` exists.
- Leave an already-current `dist` untouched on repeated generation, avoiding
  an unnecessary rename while a preview or file viewer may hold it open.
- Preserve the previous generated output on replacement failure.
- Return an actionable, path-free error if both rename and validated copy are
  denied; never expose the tester's project directory.
- Cover transient staging denial, transient and repeated rename denial,
  copy fallback, persistent-denial cleanup, and unchanged-output behavior with
  deterministic regressions.

## Release and Skill contract fixes

- Select exactly one checksum row by the full beta.9 tarball filename on
  Windows, Linux, and macOS; reject missing, duplicate, stale, and mismatched
  rows without requiring optional Release assets.
- Provide an executable PowerShell one-line receipt command.
- Make the return channel unambiguous: a private copy is for privacy triage
  only; completed Phase 6B evidence must be submitted by the independent user
  through the privacy-safe Issue Form.
- Align the bundled `build-skill-cosmos` contract with package
  `0.11.0-beta.9` and CLI compatibility family `0.4.x`, with regression tests
  tied to the actual package and executable versions.

## Independent acceptance entry points

- [Download the one-file Chinese handoff](https://github.com/Lucifer-St/silent-orbit-skills-library/releases/download/v0.11.0-beta.9/V1_RC_ONE_FILE_HANDOFF.zh-CN.md).
- [Read the full Chinese checklist](https://github.com/Lucifer-St/silent-orbit-skills-library/blob/v0.11.0-beta.9/docs/testing/v1-rc-acceptance.zh-CN.md).
- [Submit the privacy-safe V1 RC External Acceptance Issue Form](https://github.com/Lucifer-St/silent-orbit-skills-library/issues/new?template=v1_rc_acceptance.yml).

The GitHub Pre-release at [v0.11.0-beta.9](https://github.com/Lucifer-St/silent-orbit-skills-library/releases/tag/v0.11.0-beta.9) is
the only authoritative entry point. A private copy sent to the maintainer is
for privacy triage only and is not completed Phase 6B evidence.

## Compatibility and release boundary

- Package / repository: `0.11.0-beta.9`.
- CLI interface: `0.4.0`.
- Schema family: unchanged v1 contracts and hashes.
- Distribution: immutable GitHub Pre-release assets; npm remains unpublished.
- Production authority remains Public GitHub `main` after the required
  `release-gate`, consumed by the connected Netlify site.

Independent-human acceptance remains required. This candidate does not create
a v1 release or convert internal, fixture, CI, or maintainer evidence into
external acceptance.
